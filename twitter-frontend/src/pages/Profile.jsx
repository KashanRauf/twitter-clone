import React, { useEffect, useState, useContext } from "react";
import AuthContext from "../context/AuthProvider";

const Profile = () => {


  return (
    <div>Profile</div>
  )
}

export default Profile

// Refactor home page into a "base page" with the sides
// Specific pages like home and profile will justg load the relevant stuff within the center